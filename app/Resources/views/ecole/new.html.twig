{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création Ecole</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_ecole_index') }}">Retour à la liste des Ecoles</a>
        </li>
    </ul>
{% endblock %}
