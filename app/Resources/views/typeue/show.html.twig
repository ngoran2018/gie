{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeue</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeue.id }}</td>
            </tr>
            <tr>
                <th>Libtypeue</th>
                <td>{{ typeue.libtypeue }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typeue_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typeue_edit', { 'id': typeue.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
